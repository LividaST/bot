exports.run = (client, message, args) => {	
//		const apiKey = 'BLANK';
//		const telnyx = require('telnyx')(apiKey);

/*		telnyx.messages.create({
 			 'from': 'Livida',
 			 'to': '+447470967099',
 			 'text': `${message.author.tag} has just sent you a message!`,
 			 'messaging_profile_id': 'a7ad45a1-aad7-4084-8ce9-f3391fbd8d53'
		}).then(function(response){
		  const message = response.data; // asynchronously handled
	});
		
*/
	message.channel.send('Ehmm no');
};